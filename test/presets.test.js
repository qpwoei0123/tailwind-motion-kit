'use strict';

const fade = require('../src/presets/fade');
const slide = require('../src/presets/slide');
const scale = require('../src/presets/scale');

console.log({ fade: fade.name, slide: slide.name, scale: scale.name });
